import React from 'react';
import './FlexBox.css';

const FlexBox = () => {
    return (
        <>
        <div className="container">
           <div className="card">page when looking at its layout.</div>
           <div className='item'>
           <div className="card">It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout.</div>
           <div className="card">content of a page when looking at its layout.</div>
           </div>
           </div>

           
        </>
    );
};

export default FlexBox;